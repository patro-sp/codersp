#include<algorithm>

bool check=binary_search(startindex,endindex,key)==>returns boolean value..if the element is present returns true otherwise false...

auto lb=lower_bound(starting index,ending index,key)==>returns address of the element which is >= key.....
(lb-addr od first element) gives the index of first occurance of the key....

auto ub=upper_bound(starting index,ending index,key)==>returns address of the element which is >key.....
(up-addr of the first element-1)gives the index of the upper bound of the key....

(ub-lb)==>gives the frequency of the element key in the array...